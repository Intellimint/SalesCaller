#!/usr/bin/env node

// Simple build script for deployment
console.log("Starting build process...");

// For this application, we just need to ensure all static files are ready
// No complex build steps needed since we're using vanilla HTML/CSS/JS

console.log("Build completed successfully!");
console.log("Static files are ready for deployment");

process.exit(0);